#!/bin/bash
# Generate changeset
echo '📝 Generating changeset...'
npx changeset add || exit 1

# Add changeset to the commit
git add .changeset/*

# Execute commitlint
pnpm dlx commitlint --edit $1
